/**
 * Single source for the contact address.
 *
 * It was written out in both ContactMeComponent and the mailto fallback,
 * so a change in one place could silently leave the other stale.
 */
export const CONTACT_EMAIL = 'nahuelmanero@gmail.com';
